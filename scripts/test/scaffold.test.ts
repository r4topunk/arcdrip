import { describe, expect, it } from 'vitest';

// Scaffold-only: keeps `vitest run` meaningful before the real e2e helpers exist.
describe('scaffold', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
